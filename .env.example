# ============================================
# InnriGreifi Docker Compose Environment Variables
# ============================================
# Copy this file to .env and update the values as needed
# cp .env.example .env

# ============================================
# PostgreSQL Database Configuration
# ============================================
POSTGRES_USER=admin
POSTGRES_PASSWORD=password
POSTGRES_DB=innrigreifi
POSTGRES_PORT=5432

# ============================================
# Docker Image Configuration
# ============================================
# GitHub Container Registry prefix (usually 'ghcr.io')
GHCR_IMAGE_PREFIX=ghcr.io
# Your GitHub username or organization
GHCR_OWNER=YOUR_GITHUB_USERNAME
# Image tag to use (e.g., 'latest', 'v1.0.0', or a specific commit hash)
IMAGE_TAG=latest

# ============================================
# Backend Service Configuration
# ============================================
# Port to expose the backend API on the host
BACKEND_PORT=8080
# ASP.NET Core environment (Development, Staging, Production)
ASPNETCORE_ENVIRONMENT=Production
# CORS allowed origins (comma-separated list of URLs)
# Include all URLs that will access the API (frontend URLs, nginx proxy URLs, etc.)
CORS_ORIGINS=http://localhost:3000,http://localhost:80,http://localhost:443

# ============================================
# Frontend Service Configuration
# ============================================
# Port to expose the frontend on the host
FRONTEND_PORT=3000
# Base URL for the API that the frontend will use
# This should point to your nginx proxy manager or backend directly
FRONTEND_API_BASE=http://localhost:80/api

# ============================================
# Nginx Proxy Manager Configuration
# ============================================
# HTTP port for nginx proxy manager
NGINX_HTTP_PORT=80
# HTTPS port for nginx proxy manager
NGINX_HTTPS_PORT=443
# Admin UI port for nginx proxy manager
# Default login: admin@example.com / changeme (change on first login)
NGINX_ADMIN_PORT=81
